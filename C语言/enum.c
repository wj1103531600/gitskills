enum escapes { BELL = '\a', BACKSPACE = '\b', TAB = '\t',
			NEWLINE = '\n', VTAB = '\v', RETURN = '\r'
};

enum months { JAN = 1, FEB, MAR, APR, MAY, JUN,
			 JUL, AUG, SEP, OCT, NOV, DEC
};              /* FEB的值为2， MAR的值为3，等等 */ 


/* enum是枚举，默认从0开始，不断迭代，如果不是所有值都指定了，那么未指定名字的值依着最后一个指定值向后递增 */
 
